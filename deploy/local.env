# The whole stack on this machine, with Caddy's own CA:
#   cp local.env .env && docker compose up -d --build
COMPOSE_FILE=compose.yaml:compose.local.yaml

TRUSTEE_A_HOST=localhost:8441
TRUSTEE_B_HOST=localhost:8442
TRUSTEE_C_HOST=localhost:8443
CADDY_EMAIL=ops@localhost
TRUST_DOMAIN=localhost
COMPONENT_PREFIX=local-recovery-trustee
CONTACT=none
JURISDICTION=none
MIN_COOLDOWN=PT10S
